name: build-and-push

on:
  push:
    branches: [feature/auth]
  workflow_dispatch:

env:
  REGISTRY: registry.full4media.com
  IMAGE_NAME: apps/todo-app-server

jobs:
  image:
    name: Build, scan, push and update deployment
    runs-on: ubuntu-latest
    permissions:
      contents: write
      security-events: write
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from pom.xml
        id: version
        run: |
          VERSION=$(mvn -B -ntp -q help:evaluate -DforceStdout -Dexpression=project.version 2>/dev/null | tail -1)
          SHORT_SHA=${GITHUB_SHA::7}
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build application
        run: ./mvnw -B -ntp package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (local, no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          platforms: linux/amd64
          tags: localhost:5000/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Start local Docker registry
        run: |
          docker run -d -p 5000:5000 --restart=always --name registry registry:2
          sleep 5
          docker tag localhost:5000/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }} \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}

      - name: Run Trivy vulnerability scanner (table)
        id: trivy-table
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: localhost:5000/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
          format: 'table'
          output: 'trivy-report.txt'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH,MEDIUM,LOW,UNKNOWN'
          exit-code: '0'

      - name: Generate Trivy SARIF report
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: localhost:5000/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
          format: 'sarif'
          output: 'trivy-report.sarif'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH,MEDIUM,LOW,UNKNOWN'
          exit-code: '0'

      - name: Block if CRITICAL vulnerabilities
        if: always()
        run: |
          if grep -q "│         CRITICAL" trivy-report.txt 2>/dev/null; then
            echo "ERROR: CRITICAL vulnerabilities found. Blocking push."
            cat trivy-report.txt
            exit 1
          fi
          echo "No CRITICAL vulnerabilities. Push allowed."

      - name: Upload Trivy table report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report-${{ steps.version.outputs.version }}
          path: trivy-report.txt
          retention-days: 30

      - name: Upload Trivy SARIF report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-sarif-${{ steps.version.outputs.version }}
          path: trivy-report.sarif
          retention-days: 30

      - name: Upload Trivy report to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-report.sarif
          category: trivy-image-scan

      - name: Login to Zot registry
        if: success()
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Push image to Zot
        if: success()
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ steps.version.outputs.short_sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update gitops-config deployment
        if: success()
        env:
          GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          SHORT_SHA="${{ steps.version.outputs.short_sha }}"
          IMAGE="registry.full4media.com/apps/todo-app-server:${VERSION}"
          git clone "https://x-access-token:${GITOPS_TOKEN}@github.com/asixc/gitops-config.git" /tmp/gitops
          cd /tmp/gitops
          sed -i "s|image: registry.full4media.com/apps/todo-app-server:.*|image: ${IMAGE}|" \
            apps/base/todo-app/api-deployment.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add apps/base/todo-app/api-deployment.yaml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(todo-app-api): bump image to ${VERSION} (sha-${SHORT_SHA})"
            git push
          fi